### 1. 统一实验框架搭建 (Framework Architecture)

**目标**：构建一个“插拔式”的架构，确保只需通过命令行参数即可切换模型，无需改动核心逻辑。

* **参数解耦 (Arguments & Config)**：
* 在 `arguments/__init__.py` 中引入 `ModelParams` 扩展，增加 `--model_type` 参数，支持 `4dgs`、`swings`、`hybrid` 三种模式 。


* 增加 `hybrid` 专用超参数：平均位移阈值 `tau_avg`、最大瞬时位移阈值 `tau_max`、软约束权重 `lambda_d` 。




* **抽象基类设计 (Base Model Interface)**：
* 定义 `BaseGaussianModel` 接口，规范所有模型必须具备的方法：`training_setup`（优化器配置）、`capture_status`（状态保存）、`update_learning_rate`（学习率调度）以及核心的 `step_logic`（单步训练逻辑） 。




* **统一训练流 (Unified Trainer)**：
* 修改 `train.py`，根据 `model_type` 动态实例化对应的模型类。
* 实现自动记录功能：在训练开始时记录 `start_time`，结束时记录 `end_time`，并自动调用显存占用监测工具 。





### 2. 模型封装与集成 (Model Integration)

* **3D-4DGS 封装**：
* 将现有仓库代码封装为 `Gaussian4DModel` 类，作为基准（Baseline） 。




* **SWinGS 集成 (时间解耦)**：
* 引入滑动窗口逻辑：编写 `WindowScheduler`，在长序列（2-3分钟）中根据当前迭代次数控制激活的时间帧范围 $W_t$ 。


* 集成开源实现（如参考 SWinGS 官方库）：重点引入其时间一致性损失函数，确保分段训练后的帧间平滑 。




* **HybridGS 核心开发 (重点创新)**：
* 新建 `HybridGaussianModel` 类，继承自 `Gaussian4DModel`。
* 实现 **空间硬约束 (Hard Constraint)**：编写 `RigidClassifier` 模块，在指定迭代（如第3000步）触发判定。利用位移联合判定机制（$R_i < \tau_{avg} \text{ AND } R_{i,max} < \tau_{max}$）识别真背景 。


* 实现 **动态 3D 化转换**：对判定为背景的点，将其位移 MLP 的输出强制置零，并在后续优化器中剔除其动态参数的梯度更新 。





### 3. Hybrid 模型核心算法实现

* **软约束正则化 (Soft Constraint - $L_{reg\_d}$)**：
* 在 `utils/loss_utils.py` 中实现位移收敛正则项：$\mathbf{L}_{reg\_d} = \lambda_d \sum ||\mathbf{d}||_2$ 。


* 
**动态能力切换逻辑**：在训练循环中，将此 Loss 施加于被判定为前景的 4D 高斯。通过“损失函数拔河”机制，让模型在静止片段自动收敛至 3D 效果，在运动片段激活 4D 性能 。




* **SWinGS 深度集成**：
* 确保 HybridGS 能够运行在 SWinGS 提供的局部窗口架构之上，解决长序列表演视频的内存溢出问题 。





### 4. 自动化指标计算模块 (Metrics Evaluator)

* 
**基础质量指标**：集成 `PSNR`、`SSIM`、`LPIPS` 的自动计算脚本，训练完成后对验证集进行全量评估 。


* **效率评估指标**：
* 记录训练总耗时及显存峰值。
* 计算渲染帧率（FPS），验证 3D 背景冻结后的性能提升 。




* **长序列专项指标**：
* 
**Flickering Metric (闪烁度)**：计算视频序列帧间的像素一致性，量化评估时间一致性损失的效果 。





### 5. 渲染器与 3D 漫游 (Visualizer & Walkthrough)

* **离线渲染管线**：
* 编写 `render.py` 脚本，支持加载训练好的 `pth` 模型。
* 支持自定义相机路径（如 90° 至 120° 观众席视角轨迹），批量导出视频 。




* **实时交互漫游**：
* 利用 `diff-gaussian-rasterization` 后端，实现一个支持鼠标操作的 3D 场景漫游器，实时展示动态表演效果。



### 6. UI 可视化软件集成

* **GUI 开发 (基于 Dear PyGui 或类似框架)**：
* **模型训练面板**：实时可视化训练 Loss 曲线、显存占用和当前重建图预览。
* **模型对比面板**：支持分屏对比不同模型（如 HybridGS vs 4DGS）在同一帧下的渲染细节。
* 
**数据管理面板**：可视化 COLMAP 位姿预处理进度和长序列时间窗口划分情况 。

